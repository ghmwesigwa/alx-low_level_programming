
#include <stdio.h>

/**
 * main - prints lower and upper case alphabets
 *
 * Return: 0 for success
 */
int main(void)
{
	char lower = 'a';
	char upper = 'A';

	for ( ; lower <= 'z'; n++)
		putchar(lower);
	for ( ; upper <= 'Z'; m++)
		putchar(upper);
	putchar(10);
	return (0);
}
